platform :ios, '13.0'

target 'Buildio (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'SwiftyBeaver', '~> 1.9.5'
  pod 'Alamofire', '~> 4.9.0'

end

target 'Buildio (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Buildio (macOS)

end
